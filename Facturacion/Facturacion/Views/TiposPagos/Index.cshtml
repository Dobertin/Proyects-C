@model IEnumerable<Facturacion.Models.TipoPago>

<h2>Tipos de Pago</h2>

<p>
    <a href="@Url.Action("Create")">Añadir Tipo de Pago</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Nombre</td>
                <td>@item.Descripcion</td>
                <td>
                    <a href="@Url.Action("Edit", new { id = item.ID })">Editar</a> |
                    <a href="@Url.Action("Delete", new { id = item.ID })">Eliminar</a>
                </td>
            </tr>
        }
    </tbody>
</table>
